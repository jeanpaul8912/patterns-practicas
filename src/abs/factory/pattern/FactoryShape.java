package abs.factory.pattern;

public class FactoryShape extends AbstractFactory {
	/*
	@Override
	public IShape createInstanceShape(String typeEntity) {
		
		switch (typeEntity) {
		case "Rectangule": return new Rectangule();
		case "Circle": return new Circle();
		default:
			throw new IllegalArgumentException("Unexpected value: " + typeEntity);
		}
	}

	@Override
	public IColor createInstanceColor(String typeEntity) {
		return null;
	}
	*/
}